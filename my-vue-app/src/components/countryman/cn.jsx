

const Cn = ({coun}) => {
    return (
        <div>
            <h4>cn page</h4>
            <small>{coun.name.common}</small>
            {console.log(coun)}
        </div>
    );
};

export default Cn;